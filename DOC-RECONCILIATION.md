# DOC-RECONCILIATION.md

This file was generated by the `/doc-truth-up` documentation-reconciliation pass, which treats the
code as read-only ground truth and edits only documentation so it reflects the repo's actual current
state. It changed no source code, config, tests, or lockfiles, and ran no builds or tests. Its
purpose is to serve as an auditable record of what was verified, what was corrected, and what could
not be confirmed.

---

## Per-Claim Findings

### 1. What it is

**Status:** consistent  
**Evidence:** `README.md` introduction accurately describes a real-time simultaneous-resolution hex
strategy game. Entry points confirmed: `crates/battleground-{core,server,wasm}/src/` (Rust),
`client/src/main.tsx` (React). Core modules (`simulation.rs`, `combat.rs`, `hex.rs`,
`pathfinding.rs`, `los.rs`, `map_gen.rs`) all present under
`crates/battleground-core/src/`. Server entry point `crates/battleground-server/src/main.rs`
uses Axum + Tokio as documented. Client screens (`LobbyScreen.tsx`, `GameScreen.tsx`,
`DeploymentScreen.tsx`, `WaitingRoom.tsx`, `GameOverScreen.tsx`, `ReplayScreen.tsx`,
`CreateRoomDialog.tsx`) align with described game flow.

### 2. Current state

**Status:** consistent  
**Evidence:** Test counts verified by reading source — no drift from prior pass:

| Crate / package | Claimed | Actual | Evidence |
|----------------|---------|--------|----------|
| battleground-core | 114 tests | 114 | `#[test]` occurrences across 12 files in `crates/battleground-core/src/` |
| battleground-server | 62 tests | 62 | `#[test]` occurrences across 9 files in `crates/battleground-server/src/` |
| battleground-wasm | 34 tests | 34 | `#[test]` occurrences in `crates/battleground-wasm/src/lib.rs` |
| client | 120 tests | 120 | `it(` / `test(` occurrences across 16 files in `client/src/__tests__/` |
| **Total** | **330** | **330** | 114 + 62 + 34 + 120 = 330 ✓ |

Only doc files changed since the previous reconciliation pass (commits `36538cc` and `d849608` —
both `docs:` commits touching `AGENTS.md`, `DOC-RECONCILIATION.md`, and `README.md` only). No
source code was modified; implementation status is unchanged.

### 3. Stack

**Status:** consistent  
**Evidence:**
- React 19: `client/package.json` → `"react": "^19.2.0"` ✓
- Vite: `"vite": "^7.2.4"` ✓
- Zustand: `"zustand": "^5.0.11"` ✓
- Tailwind CSS 4: `"tailwindcss": "^4.1.18"` ✓
- Axum: `crates/battleground-server/Cargo.toml` → `axum = { version = "=0.8.4", features = ["ws"] }` ✓
- wasm-bindgen: `crates/battleground-wasm/` crate confirmed ✓
- `BTreeMap` determinism: `crates/battleground-core/src/simulation.rs:10` —
  `use std::collections::{BTreeMap, HashSet};`; all game-state collections use `BTreeMap` ✓
- `thiserror` for core / `anyhow` for server: confirmed in respective `Cargo.toml` files ✓

### 4. How to run

**Status:** consistent  
**Evidence:** All Makefile targets documented in README verified present in `Makefile`:
`dev`, `lean-dev`, `smoke`, `smoke-docker`, `verify`, `build`, `build-wasm`, `test`,
`clean`, `clean-heavy`, `clean-local` — all defined at `Makefile:1`. `setup.sh` exists at repo
root. `docker-compose.yml` and `Dockerfile` exist. `.codex/verify.commands` and
`.codex/scripts/run_verify_commands.sh` exist. `playwright.config.ts` referenced in `Makefile:39`
is `unverifiable` (file not in glob output) but not claimed in README prose — `make smoke` target
existence is what matters. Docker port `8080` claim is `unverifiable` without running the container
but consistent with `docker-compose.yml` presence.

### 5. Known risks / contradictions

**Status:** consistent (no code↔doc contradictions)  
**Evidence:** No divergence found between code and docs beyond what was already corrected in the
prior pass. Spot-checks:
- "No `unwrap()` in production Rust": zero matches for `unwrap()` in
  `crates/battleground-core/src/simulation.rs` ✓
- "Zero HashMap in simulation module": `simulation.rs` imports only `BTreeMap` and `HashSet`;
  no `HashMap` use in game-state logic ✓
- CI pipeline claims (`cargo clippy --workspace -D warnings`, `unwrap()` check, `HashMap` check):
  all confirmed in `.github/workflows/ci.yml` ✓

### 6. Next move / completeness

**Status:** consistent  
**Evidence:** `README.md` Siege ability row reads `Demolish (destroys forest and fortress terrain)`,
which matches `crates/battleground-core/src/simulation.rs:467` —
`if terrain == Terrain::Forest || terrain == Terrain::Fortress`. Corrected in the prior pass;
still accurate. No new stubs or TODOs introduced since last reconciliation (source code unchanged).

---

## Contradictions for Manual Review

None. All claims in editable doc files are consistent with current code. No drift requiring human
intervention was found in files outside the editable scope.

---

## Footer

**Run date/time:** 2026-06-02 19:36:38 PDT  
**Branch:** docs/truth-up-2026-06-02  
**HEAD sha reconciled against:** d849608b978c9bd4a98d7d191b718439095cfc3b
